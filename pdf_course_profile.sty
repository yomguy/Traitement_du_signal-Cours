\usepackage[pdftex]{graphicx}
\usepackage[body={17cm, 25cm}]{geometry}
\setlength{\headheight}{30pt}         % more space for the head

\usepackage[french]{babel}    % typographie française
\usepackage[latin1]{inputenc} % entree 8 bits iso-latin1
\usepackage[T1]{fontenc}      % encodage 8 bits des fontes utilisees
\usepackage{verbatim}
%\usepackage{newcent}
%\usepackage{fullpage}		% marges simplifiées
\usepackage{footnote}
\usepackage{graphicx}		% pour les figures
\usepackage{float}			% pour le positionnement des figures là où on veut
\usepackage{amsmath}		% mathematiques
\usepackage{amsfonts}
%\usepackage{newlfont}
\usepackage{mestheoremes}	% redéfinitions des théorèmes (francisés)
\usepackage[tight,centerlast,sc,small]{subfigure}
\usepackage{latexsym}
\usepackage{url}
\usepackage{setspace}
\usepackage[usenames,dvipsnames]{color}
\DeclareGraphicsExtensions{.jpg,.png,.tif,.tiff,.pdf}
\usepackage{fancyhdr}                    % Fancy Header and Footer

% style général
\parskip=5pt  % indentation de 0.5 pt
\setcounter{secnumdepth}{3}	% numérotation jusqu'à la subsubsection
%\thispagestyle{empty}
\setcounter{tocdepth}{5}

% PDF
% PDF Link colors
\definecolor{refcolor}{rgb}{0,0,0.4}    % Color for the reference links
\definecolor{anchorcolor}{rgb}{0,0,0}   % Color for anchor text
\definecolor{citecolor}{rgb}{0,0.4,0.4} % Color for bibligraphical citations
\definecolor{urlcolor}{rgb}{0,0,0.4}   % Color for linked URLs
\definecolor{filecolor}{rgb}{0.75,0,0}  % Color for URLs which open local files
\definecolor{menucolor}{rgb}{0.75,0,0}  % Color for Acrobat menu items
\definecolor{pagecolor}{rgb}{0.75,0,0}  % Color for links to other pages

\usepackage[pdftex,bookmarksopen,bookmarksnumbered,colorlinks,hyperindex,pagebackref,breaklinks,
pdftitle={Cours de Traitement du signal - CNAM - CPDA},
pdfauthor={Guillaume Pellerin and Maëva Garnier},
pdfsubject={Introduction aux méthodes de traitement du signal},
pdfkeywords={traitement, signal, transformée, Fourier, filtrage, représentation, CNAM, CPDA},
hyperfigures=true,          % provide links to figures
hyperindex=true,           % provide links from index to pages (broken?)
pagebackref=true,           % provide links to pages where bib is cited from
backref=true,               % provide links to sec where bib is cited from
colorlinks=true,            %***
linkcolor=refcolor,         %
anchorcolor=anchorcolor,    %
citecolor=citecolor,        % Colors are defined above;
filecolor=filecolor,        % change them there
menucolor=menucolor,        %
pagecolor=pagecolor,        %
urlcolor=urlcolor,          %***
bookmarksopen=true,        % whether to open bookmarks window on open
pdfpagemode=UseOutlines,    % show the bookmarks in outline form
pdfstartview=FitB,          % default window sizing
pdfview=FitB]{hyperref}     % default view after clicking a link

    %%% Fancy Header %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    % Fancy Header Style Options
\pagestyle{fancy}                       % Sets fancy header and footer
\fancyfoot{}                            % Delete current footer settings
\renewcommand{\chaptermark}[1]{         % Lower Case Chapter marker style
\markboth{\chaptername\ \thechapter.\ #1}{}} %
\renewcommand{\sectionmark}[1]{        % Lower case Section marker style
\markright{\thesection.\ #1}}         %
\fancyhead[LE,RO]{\bfseries\thepage}    % Page number (boldface) in left on even
                                            % pages and right on odd pages
\fancyhead[RE]{\footnotesize{\bfseries\leftmark}}      % Chapter in the right on even pages
\fancyhead[LO]{\footnotesize{\bfseries\rightmark}}     % Section in the left on odd pages
\renewcommand{\headrulewidth}{0.3pt}    % Width of head rule

%pb odd/even pages with twosides
\setlength{\oddsidemargin}{0cm}
\setlength{\evensidemargin}{\paperwidth}
\addtolength{\evensidemargin}{-\textwidth}
\addtolength{\evensidemargin}{-2.0in}
\addtolength{\evensidemargin}{-\oddsidemargin}

%Titre
\renewcommand{\maketitle}{
    \begin{titlepage}
    \vspace{5cm}
       \textsf{\@title}
        \centering
        \vspace{1cm}
        \large{\@author}
        \rule{17cm}{0.5mm}\\
        \small{Versions électroniques au format PDF avec hypertexte :}\\
        \footnotesize{\url{http://www.cnam.fr/cpda/docscours/3A/PELLERIN/Signal-CPDA-CNAM.pdf}}\\
		\footnotesize{\url{http://files.parisson.com/CNAM/Signal-CPDA-CNAM.pdf}}
    \end{titlepage}
}


% newcommand texte
\newcommand{\be}{\begin{enumerate}}  % raccourci liste numérotée début
\newcommand{\ee}{\end{enumerate}}    % raccourci liste numérotée fin
%\newcommand{\bold}[1]{\textbf{#1}}   % racourci texte en gras
\newcommand{\cad}{c'est-à-dire }

% newcommand mathématique
\newcommand{\rad}{\mathop{\mathrm{rad}}}
\newcommand{\radn}{\mathop{\mathrm{rad}}\nolimits}
\newcommand{\ud}{\mathrm{d}} 		 % élément différientiel en caractères romains
\newcommand{\fractext}[2]{\frac{\displaystyle #1}{\displaystyle #2}}
\newcommand{\grad} {\overrightarrow{grad}}    % gradient avec une longue flèche
\newcommand{\boxeq}[1]{\fbox{$\displaystyle{#1}$}}

%\renewcommand{\floatpagefraction}{0.9}
%\renewcommand{\textfraction}{0.1}
%\setlength{\abovecaptionskip}{0.2cm}
%\setlength{\belowcaptionskip}{0.2cm}
% Different font in captions
\newcommand{\captionfonts}{\small\it}

\makeatletter  % Allow the use of @ in command names
\long\def\@makecaption#1#2{%
  \vskip\abovecaptionskip
  \sbox\@tempboxa{{\captionfonts #1: #2}}%
  \ifdim \wd\@tempboxa >\hsize
    {\captionfonts #1: #2\par}
  \else
    \hbox to\hsize{\hfil\box\@tempboxa\hfil}%
  \fi
  \vskip\belowcaptionskip}
\makeatother   % Cancel the effect of \makeatletter


% macros

%%%% debut macro page vierge avant chapitre%%%%
% whitecdp (formerly schulzrinne.sty) --provide for blank pages
% between chapters
% This redefinition of the \cleardoublepage command provides
% for a special pagestyle for the "extra" pages which are generated
% to ensure that the chapter opener is on a recto page.
% The pagestyle is "chapterverso"; for many publishers, this should be
% identical to "empty", so that's the default.
\makeatletter
\def\cleardoublepage{\clearpage
 \if@twoside
  \ifodd\c@page\else
   \null\thispagestyle{chapterverso}\newpage
   \if@twocolumn\null\newpage\fi
   \fi
  \fi
 }%
\def\ps@chapterverso{\ps@empty}%
\makeatother
%%%% fin macro %%%%

% http://www.ctan.org/tex-archive/macros/eplain/
% pour faire une jolie fraction 1/2
\def\fract#1/#2{\leavevmode
   \kern.1em \raise .5ex \hbox{\the\scriptfont0 #1}%
   \kern-.1em $/$%
   \kern-.15em \lower .25ex \hbox{\the\scriptfont0 #2}%
}%


